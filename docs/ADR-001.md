# ADR-001: Blueprint-first agent delivery with topic evals

## Context
Copilot Studio agents drift as topics and knowledge grow.

## Decision
Ship a versioned blueprint (topics/entities/knowledge/guardrails) plus a golden-utterance
regression set that runs before every publish.

## Consequences
- Topic changes are reviewable diffs, not clicks in a UI.
- Knowledge updates trigger re-evaluation.
